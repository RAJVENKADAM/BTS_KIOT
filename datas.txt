INCOMPLETE: unable to finish extracting COMPLETE backend-required SQL schema.

Known from migrations + inspected backend files:

Tables and minimal required columns:
1) users
- Used columns: id, name, email, password_hash, role, bus_no, is_active, temp_password, deleted_by_user, created_at, push_token (ensured by notificationService)

2) buses
- Used columns: id, bus_no, preview_number, gps_device_id, reg_no, bus_name, status, current_plan, mobile_live, operating_bus_id, combined_buses

3) bus_routes
- Used columns: id (if exists), bus_id, plan_name, stop_name, stop_order

4) bus_live_locations
- Used columns: id, bus_id, latitude, longitude, speed, accuracy(optional), user_id, is_online, updated_at

5) bus_states
- Used columns: id(optional), bus_no or bus_id (see bug), state, last_latitude, last_longitude, last_coords_time, state_changed_at, repeat_count(optional), updated_at

6) messages
- Used columns: id, bus_no, plan_name, message, created_by, created_at

CRITICAL SCHEMA BUG:
- Code in busStateService.js expects bus_states columns bus_id and joins with buses.id.
- Your existing migration 012_add_bus_states_table.sql uses bus_no (not bus_id) and UNIQUE(bus_no).
- This causes 'Unknown column bs.bus_id' crash.

Next required step (not completed):
- Full scan of backend/src for ALL SQL queries and extract ALL referenced columns, keys, and constraints.

Reason not completed:
- Attempted a JS scan via node in Windows shell; command failed due to '&&' parsing issue.

If you want, I can retry scanning using a single command without '&&' or by manually reading remaining backend files, then generate the full final schema + CREATE/ALTER/migration SQL in the exact structure you requested.
