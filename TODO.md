# TODO

- [x] Initial repo scan for AddUsers UI + existing excel upload APIs.
- [ ] Add backend endpoints to delete/deactivate user and edit user fields (superadmin only).
- [ ] Add frontend `UserCard` component with Delete/Edit actions.
- [ ] Update `AddUsersScreen` to fetch and render created users (cards) for each uploaded Excel file.
- [ ] Wire delete/edit to backend and refresh users list after mutation.
- [ ] Manual test: import users → cards appear → edit/delete works.

